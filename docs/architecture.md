# Architecture

Document the current project architecture here once a concrete project exists.

Keep this file focused on information that is useful across sessions and is not obvious from the source tree itself, for example:

- major components and responsibilities;
- important data flows;
- external systems and trust boundaries;
- deployment/runtime topology;
- constraints that affect implementation choices.

Do not turn this file into a duplicate inventory of files that an agent can inspect directly.
